Listing Ideas <br />

  <%= @ideas.count %><br />

  <% @ideas.each do |ideas| %>
       ID: <%= ideas.id%>
       Title:
       <%= link_to (ideas.title), idea_path(ideas) %>
       Desc:<%= ideas.description%>
       <%= link_to  "Delete idea", idea_path(ideas) , method: :delete%><br />
  <%end%>

<%= link_to "New Idea", new_idea_path %>

