Listing Ideas <br />

  <%= @ideas.count %><br />

  <% @ideas.each do |ideas| %>
       ID: <%= ideas.id%>
       Title:
       <%= link_to (ideas.title), idea_path(ideas) %>
       Desc:<%= ideas.description%><br />
  <%end%>

<%= link_to "New Idea", ideas_new_path %>

